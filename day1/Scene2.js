const Scene2 = (char1, char2, char3) => {
  const heading = "Scene 2: Journey of Discovery";
  const str = `${char1} and ${char2} set out on a journey across distant villages to understand their people and their struggles. During this voyage, they meet the graceful and fearless ${char3}, whose kindness captures ${char1}’s heart.`;

  return `${heading}\n${str}`;
};

export default Scene2;
