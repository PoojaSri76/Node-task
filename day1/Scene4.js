const Scene4 = (char1, char2) => {
  const heading = "Scene 4: The Betrayal";
  const str = `In a heartbreaking twist of fate, ${char2} is bound by duty to kill ${char1}. With tears in his eyes, ${char2} fulfills the command, unaware that this act will haunt the kingdom and echo through generations.`;

  return `${heading}\n${str}`;
};

export default Scene4;
