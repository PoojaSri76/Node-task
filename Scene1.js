const Scene1 = (char1, char2, char3) => {
  const recap = "Scene 1: The Battle for Mahishmati";
  const str = `${char1} and ${char2} unite their armies to defend the Mahishmati kingdom from the ruthless warlord ${char3}.`;

  return `${recap}\n${str} Their courage and valor bring glory to the land and forge an unbreakable bond of brotherhood.`;
};

export default Scene1;
